/*
 * Use Call by Name to write Boolean functions
 *  and and or that implement the typical binary
 *   Boolean operators using short-circuit evaluation;
 *    that is, they only evaluate their second parameter
 *     if necessary.  Do this without using && or || (looks
 *      like & and | are actually bitwise operators, so don’t
 *       use those either.  :)
 */

object problem3 extends App{
  def problem(xs:List )
}